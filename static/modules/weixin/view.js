define(["js/views/base"], function(Base) {
    var View = Base.extend({
        moduleName: "weixin",
        init: function() {
            // //- var appId=config.appId

        }
    });
    return View;
});